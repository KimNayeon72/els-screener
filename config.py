# -*- coding: utf-8 -*-
"""
ELS 자동 스크리닝 설정 파일

사용자가 직접 채워야 하는 값은 "TODO"로 표시되어 있습니다.
"""

import os

# ---------------------------------------------------------------------------
# 1. 스크리닝 대상 판매사
# ---------------------------------------------------------------------------
TARGET_DEALERS = ["키움증권", "NH투자증권"]

# ---------------------------------------------------------------------------
# 2. 스크리닝 조건
# ---------------------------------------------------------------------------
REQUIRED_ASSET_COUNT = 2          # 기초자산 개수 (2개 구성만)
MIN_ANNUAL_YIELD_PCT = 15.0       # 최소 연 환산 수익률(%)
LOOKBACK_YEARS = 2                # 배리어 하락 이력 확인 기간(년)
# 첫 조기상환 배리어는 키움증권 공식 페이지에서 실제 값을 매칭해서 사용합니다.
# 매칭이 안 되는 상품(키움 매칭 실패, 모든 NH 상품)은 부정확한 값을 임의로
# 적용하지 않고 '확인불가'로 표시되어 자동추천에서 제외됩니다.

# ---------------------------------------------------------------------------
# 3. KRX 파생결합증권 통합정보플랫폼(data.krx.co.kr) 요청 파라미터
# ---------------------------------------------------------------------------
# data.krx.co.kr은 화면마다 고유한 'bld'(block id) 값을 알아야 JSON을 받아올 수 있습니다.
# 확인 방법:
#   1) 크롬으로 https://tip.krx.co.kr/TIP010100.cmd 접속
#   2) F12(개발자도구) > Network 탭 > XHR 필터
#   3) "조회" 버튼 클릭 → generate.cmd 로 시작하는 요청을 클릭
#   4) Payload 탭에서 'bld' 값을 복사해서 아래에 붙여넣기
KRX_GENERATE_URL = "https://data.krx.co.kr/comm/bldAttendant/getJsonData.cmd"
KRX_BLD = os.environ.get("KRX_BLD", "dbms/MDC_OUT/STAT/standard/MDCSTAT17302_OUT")

# ---------------------------------------------------------------------------
# 4. 기초자산 이름 → 시세 조회용 티커 매핑
# ---------------------------------------------------------------------------
# ELS 공시에 한글명으로 나오는 기초자산을 야후파이낸스 등에서 조회 가능한 티커로 변환합니다.
# 새로운 기초자산이 나올 때마다 이 표에 추가해야 합니다. (개별 해외종목이 특히 많음)
ASSET_TICKER_MAP = {
    "코스피200": "^KS200",
    "KOSPI200": "^KS200",
    "S&P500": "^GSPC",
    "SPX": "^GSPC",
    "EURO STOXX50": "^STOXX50E",
    "유로스탁스50": "^STOXX50E",
    "HSCEI": "^HSCE",
    "항셍중국기업지수": "^HSCE",
    "니케이225": "^N225",
    "NIKKEI225": "^N225",
    "나스닥100": "^NDX",
    "NASDAQ100": "^NDX",
    "삼성전자": "005930.KS",
    "테슬라": "TSLA",
    "엔비디아": "NVDA",
    "애플": "AAPL",
    "팔란티어 테크": "PLTR",
    "마이크론 테크놀로지": "MU",
    "AMD": "AMD",
    "인텔": "INTC",
    # 2026-09-17 실제 KRX 조회 데이터에서 나온 기초자산들 (영문 표기가 원본과 동일해야 매칭됨)
    "EUROSTOXX50": "^STOXX50E",
    "INTEL": "INTC",
    "LG전자": "066570.KS",
    "MICRON": "MU",
    "PLANTIR": "PLTR",       # KRX 원본 표기 그대로(팔란티어의 오기로 추정)
    "PALANTIR": "PLTR",
    "SK하이닉스": "000660.KS",
    "TESLA": "TSLA",
    "삼성전기": "009150.KS",
    "현대차": "005380.KS",
    # KOSDAQ150은 야후파이낸스에 정확한 지수 티커가 없어 ETF(KODEX코스닥150)로 근사.
    # 정확도가 필요하면 직접 확인 후 수정하세요.
    "KOSDAQ150": "229200.KS",
    # TODO: 새로 나오는 기초자산은 여기에 계속 추가 (엑셀에서 "판정불가"로 표시된 이름을 확인)
}

# ---------------------------------------------------------------------------
# 5. 결과 파일 저장 위치
# ---------------------------------------------------------------------------
# 이메일은 보내지 않습니다. GitHub Actions가 이 폴더의 엑셀 파일을
# 'Artifacts'(첨부파일 보관함)에 올려두면, Actions 탭에서 직접 다운로드하시면 됩니다.
OUTPUT_DIR = "output"
